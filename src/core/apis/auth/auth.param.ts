export interface LoginRequest {
    email: string;
    password: string;
}
  
export interface RegisterRequest {
    id: string;
    name: string;
    email: string;
    password: string;
    number: string;
}